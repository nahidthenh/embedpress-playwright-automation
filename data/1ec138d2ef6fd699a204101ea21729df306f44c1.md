# Test info

- Name: authenticate
- Location: /home/runner/work/embedpress-playwright-automation/embedpress-playwright-automation/tests/auth.setup.js:9:1

# Error details

```
Error: locator.fill: Test timeout of 60000ms exceeded.
Call log:
  - waiting for getByLabel('Username or Email Address')

    at /home/runner/work/embedpress-playwright-automation/embedpress-playwright-automation/tests/auth.setup.js:12:56
```

# Page snapshot

```yaml
- heading "Error establishing a database connection" [level=1]
```

# Test source

```ts
   1 | const { test: setup, expect } = require('@playwright/test');
   2 | const path = require('path');
   3 |
   4 | const authFile = path.join(__dirname, '../playwright/.auth/user.json');
   5 |
   6 | const username = `${process.env.USERNAME}`;
   7 | const password = `${process.env.PASSWORD}`;
   8 |
   9 | setup('authenticate', async ({ page }) => {
  10 |     // Perform authentication steps. Replace these actions with your own.
  11 |     await page.goto('https://embedpress.wpqa.site/wp-login.php');
> 12 |     await page.getByLabel('Username or Email Address').fill(username);
     |                                                        ^ Error: locator.fill: Test timeout of 60000ms exceeded.
  13 |     await page.getByLabel('Password', { exact: true }).fill(password);
  14 |     await page.getByRole('button', { name: 'Log In' }).click();
  15 |     await page.waitForURL('https://embedpress.wpqa.site/wp-admin/');
  16 |     await expect(page.getByRole('heading', { name: 'Dashboard' })).toBeVisible();
  17 |
  18 |     // End of authentication steps.
  19 |     await page.context().storageState({ path: authFile });
  20 | });
```