# Test info

- Name: Classic Google drawings >> Classic Google drawings
- Location: /home/runner/work/embedpress-playwright-automation/embedpress-playwright-automation/tests/classic/google-drawings.spec.js:11:5

# Error details

```
Error: Timed out 5000ms waiting for expect(locator).toBeVisible()

Locator: getByRole('heading', { name: 'Classic Google Drawings' })
Expected: visible
Received: <element(s) not found>
Call log:
  - expect.toBeVisible with timeout 5000ms
  - waiting for getByRole('heading', { name: 'Classic Google Drawings' })

    at /home/runner/work/embedpress-playwright-automation/embedpress-playwright-automation/tests/classic/google-drawings.spec.js:13:86
```

# Page snapshot

```yaml
- heading "Error establishing a database connection" [level=1]
```

# Test source

```ts
   1 | const { test, expect } = require('@playwright/test');
   2 |
   3 | const slug = 'playwright-classic-editor/classic-google-drawings/';
   4 |
   5 | test.describe("Classic Google drawings", () => {
   6 |     test.beforeEach(async ({ page }) => {
   7 |         await page.goto(slug);
   8 |         await page.waitForLoadState('networkidle');
   9 |     });
  10 |
  11 |     test('Classic Google drawings', async ({ page }) => {
  12 |         // Check iframe visibility        
> 13 |         await expect(page.getByRole('heading', { name: 'Classic Google Drawings' })).toBeVisible();
     |                                                                                      ^ Error: Timed out 5000ms waiting for expect(locator).toBeVisible()
  14 |
  15 |         // Check iframe visibility
  16 |         const iframe = page.locator('#post-9213').getByRole('img');
  17 |         await expect(iframe).toBeVisible();
  18 |         
  19 |         // Check dimensions height & width
  20 |         const { height, width } = await iframe.evaluate(iframe => iframe.getBoundingClientRect());
  21 |         expect(height).toBeCloseTo(375, 1);
  22 |         expect(width).toBeCloseTo(500, 1);
  23 |     });
  24 | });
  25 |
```